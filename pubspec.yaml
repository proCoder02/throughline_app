name: throughline
description: Throughline mobile client (Flutter)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.3
  web_socket_channel: ^2.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.2
  intl: ^0.19.0
  record: ^7.1.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
